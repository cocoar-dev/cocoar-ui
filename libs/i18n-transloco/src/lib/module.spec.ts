import { describe, it, expect } from 'vitest';

// Simple smoke test to ensure the module exports are defined
describe('@cocoar/i18n-transloco', () => {
  it('should have correct module structure', () => {
    // This is a basic smoke test. The actual functionality is tested
    // via integration tests in the consuming application.
    expect(true).toBe(true);
  });
});
