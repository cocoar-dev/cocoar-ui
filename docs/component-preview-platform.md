# Component Preview Platform (Working Title)

## Summary

We are building a small, dedicated Angular application that can **render exactly one component scenario in isolation** (similar to a Storybook “preview iframe”) and make it available for:

- **automated UI tests** (Playwright)
- **manual exploration** (developers/designers)
- **future showcase integration** (documentation + live preview in a dedicated tab)

This repo intentionally avoids Storybook, so this platform is our **controlled, Angular-first alternative**.

---

## Why This Exists

### Problem

We need reliable component-level tests and reproducible demos. Using a full showcase route for testing introduces:

- test flakiness from unrelated page layout and navigation
- selectors polluted by “showcase chrome”
- higher maintenance cost as demo pages evolve

Storybook typically solves this by rendering each story in its own deterministic iframe.

### Solution

Create a **minimal preview host** that mounts one scenario at a time via a stable URL contract:

- `/<preview-route>/:id` mounts exactly one scenario
- query parameters configure a small allowlist of inputs
- optional per-scenario providers support overlays, i18n stubs, etc.

This becomes the stable surface for Playwright and a consistent “try it out” UI.

---

## What It Does (Today)

### Core capabilities

- **Isolated rendering**: one scenario per route
- **Lazy loading**: scenarios are loaded by dynamic `import()`
- **URL-driven inputs**: query params → typed parsing → component inputs
- **Provider overrides**: optional per-scenario providers
- **Auto-discovery**: scenario registry is generated by scanning the repo
- **Guardrails**: runtime code cannot import scenario metadata directly (enforced)

### Authoring model

Instead of rendering a design-system component directly, we author **wrapper components** (“scenarios”) that can:

- handle content projection
- include small layouts
- stitch multiple components together
- include overlays or templates

This mirrors how Storybook encourages “stories” as templates, but stays 100% Angular.

---

## What It Enables

### 1) Isolated component testing

Playwright tests become direct and stable:

- Navigate to a deterministic URL
- Assert behavior with minimal DOM noise
- Keep tests focused on one component state

### 2) Manual component exploration

Developers can open a scenario URL and tweak inputs via query params.

### 3) Future: Showcase integration

Long-term, we can integrate isolated previews into the existing showcase app without turning the showcase into the test harness.

Conceptually:

- Showcase keeps docs, examples, API tables, guidelines
- The preview platform provides the **live isolated preview**
- The showcase embeds the preview in a dedicated “Preview” tab (likely an iframe)

This keeps the testing surface stable even when docs UX changes.

---

## Non-Goals (For Now)

To keep the platform maintainable, we are intentionally *not* implementing Storybook’s full feature set.

Examples of non-goals (for early versions):

- addons ecosystem
- complex UI for “controls” panels (beyond URL params)
- visual regression tooling built-in
- automatic docs generation from TS types (beyond what we already infer)

These can be evaluated later if there is a clear ROI.

---

## Roadmap (High Level)

### Near-term

- Expand scenario coverage for key components
- Add more Playwright scenarios and helpers
- Make the tooling portable to non-Nx Angular CLI repos

### Mid-term

- Provide a small “scenario explorer” UI (optional)
- Integrate a preview tab into showcase (embed isolated preview)
- Improve type inference and support more Angular input patterns

### Long-term

- Turn this into a reusable internal product (dedicated repo)
- Provide an installer/CLI to bootstrap the preview platform in any Angular project

---

## Naming & Terminology

### Why rename now

`CtHost`, `CtStory`, etc. were good bootstrap names, but if this grows into a real internal product, we should use names that communicate:

- **preview** (interactive rendering)
- **scenarios** (reproducible component states)
- **testing** (stable target surface)
- **platform** (reusable, installable)

### Suggested product names (candidates)

These are intentionally neutral and “enterprise-friendly”. Pick one based on what resonates with your stakeholders.

- **Coar Preview Platform**
  - emphasizes the future (embedded previews + testing surface)
- **Coar Component Preview**
  - very literal; easy to explain
- **Coar Component Lab**
  - implies experimentation and exploration
- **Coar UI Sandbox**
  - familiar concept for devs; highlights isolation
- **Coar UI Harness**
  - strong testing connotation; less “product UI” connotation

If you want to avoid “Coar” in the product name (for cross-brand use), drop it:

- **Component Preview Platform**
- **UI Scenario Host**
- **Component Sandbox**

### Suggested terminology inside code

Replace “CT” with language that maps to the concepts:

- **Host/App**
  - `PreviewHost` / `PreviewApp` / `PreviewShell`
- **Story** (Storybook term) → consider **Scenario** or **Fixture**
  - `Scenario` reads well for stakeholders
  - `Fixture` reads well for testing-focused teams
- **Registry**
  - `ScenarioRegistry` / `PreviewRegistry`
- **Route**
  - `/__preview/:id` or `/__scenario/:id` (explicitly internal)

### Naming recommendations (my vote)

If the future includes a showcase “Preview” tab and a testing surface, the cleanest mental model is:

- Product: **Coar Preview Platform**
- Unit: **Scenario**
- App: **Preview Host**

So:

- `PreviewHost`
- `ScenarioDefinition<T>` (instead of `CtStory<T>`)
- `PREVIEW_REGISTRY` / `SCENARIO_REGISTRY`

This keeps “story” out of the vocabulary while still matching what Storybook provides.

---

## Stakeholder Explanation (Short Pitch)

“We’re building a lightweight preview platform for our UI components. Each component state is rendered in isolation via a stable URL, which gives us a deterministic surface for Playwright tests and an interactive preview that can later be embedded into the showcase app. It’s like Storybook’s preview iframe concept, but implemented in a controlled Angular-first way and tailored to our design system.”
